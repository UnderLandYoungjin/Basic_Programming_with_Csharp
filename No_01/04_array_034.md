# 🟣 C# 제4강 — 배열 (Array)

## 📌 개요

지금까지는 변수 하나에 값 하나를 저장했습니다.  
그런데 학생 5명의 점수를 저장하려면 변수가 5개 필요합니다.

```csharp
int score1 = 90;
int score2 = 85;
int score3 = 78;
int score4 = 92;
int score5 = 88;
```

이렇게 하면 학생이 100명이면 변수도 100개가 필요합니다.  
**배열(Array)** 을 사용하면 같은 자료형의 값 여러 개를 **하나의 변수**에 묶어서 저장할 수 있습니다.

```csharp
int[] scores = { 90, 85, 78, 92, 88 }; // 변수 하나로 5개 저장!
```

---

## 1. 배열 선언과 초기화

### 📌 빈 배열 만들기

```
자료형[] 변수이름 = new 자료형[칸 수];
```

```csharp
int[]    scores = new int[5];     // 정수 5칸짜리 배열
string[] names  = new string[3];  // 문자열 3칸짜리 배열
```

> 💡 빈 배열을 만들면 각 칸은 자동으로 기본값이 들어갑니다.  
> `int` → `0` / `string` → `null` / `bool` → `false`

---

### 📌 값과 함께 초기화 ✅ 권장

선언할 때 바로 값을 넣을 수 있습니다.  
중괄호 `{ }` 안에 값을 콤마로 구분하여 나열합니다.

```csharp
int[]    scores = { 90, 85, 78, 92, 88 };
string[] names  = { "홍길동", "김철수", "이영희" };
```

---

## 2. 배열 요소 접근 (인덱스)

배열의 각 칸에는 **번호(인덱스)** 가 있습니다.  
인덱스는 **0부터 시작**합니다.

```csharp
int[] scores = { 90, 85, 78, 92, 88 };
//  인덱스:      [0] [1] [2] [3] [4]
```

### 📌 요소 읽기

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] scores = { 90, 85, 78, 92, 88 };

        Console.WriteLine(scores[0]); // 90  (첫 번째)
        Console.WriteLine(scores[1]); // 85  (두 번째)
        Console.WriteLine(scores[4]); // 88  (마지막)
    }
}
```

**실행 결과**
```
90
85
88
```

---

### 📌 요소 수정

인덱스를 지정하면 값을 바꿀 수 있습니다.

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] scores = { 90, 85, 78, 92, 88 };

        scores[2] = 100;  // 세 번째 칸 78 → 100으로 변경

        Console.WriteLine(scores[2]); // 100
    }
}
```

**실행 결과**
```
100
```

> ⚠️ **주의:** 존재하지 않는 인덱스를 사용하면 오류가 납니다.
> ```csharp
> int[] arr = { 1, 2, 3 };   // 인덱스: 0, 1, 2
> Console.WriteLine(arr[5]); // ❌ 오류! 인덱스 5는 없음
> ```

---

## 3. 배열의 길이 (Length)

`.Length` 속성으로 배열에 몇 개의 값이 들어있는지 알 수 있습니다.

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] scores = { 90, 85, 78, 92, 88 };

        Console.WriteLine(scores.Length); // 5
    }
}
```

**실행 결과**
```
5
```

---

## 4. 배열과 반복문

배열의 진짜 장점은 **반복문과 함께** 쓸 때 나타납니다.  
모든 요소를 하나씩 꺼내서 처리할 수 있습니다.

### 📌 for 반복문 — 인덱스가 필요할 때

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] scores = { 90, 85, 78, 92, 88 };

        for (int i = 0; i < scores.Length; i++)
        {
            Console.WriteLine($"{i + 1}번 학생: {scores[i]}점");
        }
    }
}
```

**실행 결과**
```
1번 학생: 90점
2번 학생: 85점
3번 학생: 78점
4번 학생: 92점
5번 학생: 88점
```

---

### 📌 foreach 반복문 — 값만 꺼낼 때 ✅ 권장

인덱스 없이 값을 하나씩 꺼내 쓸 때 더 간결합니다.

```
foreach (자료형 변수 in 배열이름)
{
    // 반복할 코드
}
```

```csharp
using System;
class Hello
{
    public static void Main()
    {
        string[] names = { "홍길동", "김철수", "이영희" };

        foreach (string name in names)
        {
            Console.WriteLine($"이름: {name}");
        }
    }
}
```

**실행 결과**
```
이름: 홍길동
이름: 김철수
이름: 이영희
```

> 💡 **for vs foreach 선택 기준**
> - 인덱스(번호)가 필요하다 → `for`
> - 값만 하나씩 읽으면 된다 → `foreach` (더 간단)

---

## 5. 배열 합계와 평균 구하기

배열과 반복문을 함께 사용하는 가장 기본적인 예입니다.

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] scores = { 90, 85, 78, 92, 88 };
        int sum = 0;

        foreach (int score in scores)
        {
            sum += score;  // 모든 점수를 더함
        }

        double average = (double)sum / scores.Length;

        Console.WriteLine($"합계: {sum}");
        Console.WriteLine($"평균: {average:F1}");
    }
}
```

**실행 결과**
```
합계: 433
평균: 86.6
```

> 💡 `(double)sum` 으로 형 변환을 해야 소수점 평균이 나옵니다.  
> 변환하지 않으면 `int / int` 계산이 되어 소수점이 버려집니다.

---

## 6. 배열 최댓값 / 최솟값 찾기

첫 번째 값을 기준으로 놓고, 나머지와 하나씩 비교합니다.

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] scores = { 90, 85, 78, 92, 88 };

        int max = scores[0]; // 일단 첫 번째를 최댓값으로 설정
        int min = scores[0]; // 일단 첫 번째를 최솟값으로 설정

        for (int i = 1; i < scores.Length; i++)
        {
            if (scores[i] > max) max = scores[i]; // 더 크면 교체
            if (scores[i] < min) min = scores[i]; // 더 작으면 교체
        }

        Console.WriteLine($"최댓값: {max}");
        Console.WriteLine($"최솟값: {min}");
    }
}
```

**실행 결과**
```
최댓값: 92
최솟값: 78
```

---

## 7. 배열 정렬 (Array.Sort / Array.Reverse)

### 📌 Array.Sort() — 오름차순 정렬

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] scores = { 90, 85, 78, 92, 88 };

        Array.Sort(scores); // 작은 수부터 정렬

        foreach (int s in scores)
        {
            Console.Write(s + " ");
        }
    }
}
```

**실행 결과**
```
78 85 88 90 92 
```

---

### 📌 Array.Reverse() — 순서 뒤집기

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] scores = { 90, 85, 78, 92, 88 };

        Array.Sort(scores);    // 먼저 오름차순 정렬
        Array.Reverse(scores); // 그 다음 뒤집기 → 내림차순

        foreach (int s in scores)
        {
            Console.Write(s + " ");
        }
    }
}
```

**실행 결과**
```
92 90 88 85 78 
```

---

## 8. 배열 vs 변수 비교

| 구분 | 일반 변수 | 배열 |
|---|---|---|
| 저장 가능한 값의 수 | 1개 | 여러 개 |
| 선언 예시 | `int score = 90;` | `int[] scores = { 90, 85, 78 };` |
| 값 접근 방법 | 변수명 | `배열명[인덱스]` |
| 개수 확인 | 없음 | `.Length` |
| 반복 처리 | 하나씩 직접 | `for`, `foreach` 활용 |

---

## 🧪 예제

### 예제 1 — 과일 목록 출력

```csharp
using System;
class Hello
{
    public static void Main()
    {
        string[] fruits = { "사과", "바나나", "포도", "딸기", "수박" };

        Console.WriteLine("=== 과일 목록 ===");
        for (int i = 0; i < fruits.Length; i++)
        {
            Console.WriteLine($"{i + 1}번: {fruits[i]}");
        }
        Console.WriteLine($"총 {fruits.Length}가지 과일");
    }
}
```

**실행 결과**
```
=== 과일 목록 ===
1번: 사과
2번: 바나나
3번: 포도
4번: 딸기
5번: 수박
총 5가지 과일
```

---

### 예제 2 — 성적 통계 프로그램

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] scores = { 85, 92, 78, 96, 73 };

        int sum = 0;
        int max = scores[0];
        int min = scores[0];

        foreach (int score in scores)
        {
            sum += score;
            if (score > max) max = score;
            if (score < min) min = score;
        }

        double avg = (double)sum / scores.Length;

        Console.WriteLine("=== 성적 통계 ===");
        Console.WriteLine($"학생 수: {scores.Length}명");
        Console.WriteLine($"합계:    {sum}점");
        Console.WriteLine($"평균:    {avg:F1}점");
        Console.WriteLine($"최고:    {max}점");
        Console.WriteLine($"최저:    {min}점");
    }
}
```

**실행 결과**
```
=== 성적 통계 ===
학생 수: 5명
합계:    424점
평균:    84.8점
최고:    96점
최저:    73점
```

---

### 예제 3 — 점수 정렬 출력

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] scores = { 85, 92, 78, 96, 73 };

        Console.Write("정렬 전: ");
        foreach (int s in scores) { Console.Write(s + " "); }
        Console.WriteLine();

        Array.Sort(scores);
        Console.Write("오름차순: ");
        foreach (int s in scores) { Console.Write(s + " "); }
        Console.WriteLine();

        Array.Reverse(scores);
        Console.Write("내림차순: ");
        foreach (int s in scores) { Console.Write(s + " "); }
    }
}
```

**실행 결과**
```
정렬 전: 85 92 78 96 73 
오름차순: 73 78 85 92 96 
내림차순: 96 92 85 78 73 
```

---

## 🔍 배열 요약 정리

| 구분 | 작성 방법 |
|---|---|
| 빈 배열 선언 | `int[] arr = new int[5];` |
| 값과 함께 선언 | `int[] arr = { 1, 2, 3, 4, 5 };` |
| 요소 접근 | `arr[0]`, `arr[1]` ... (인덱스는 0부터) |
| 길이 확인 | `arr.Length` |
| for 반복 | `for (int i = 0; i < arr.Length; i++)` |
| foreach 반복 | `foreach (int x in arr)` |
| 오름차순 정렬 | `Array.Sort(arr)` |
| 내림차순 정렬 | `Array.Sort(arr);` 후 `Array.Reverse(arr);` |

---

## 📝 문제

---

### 문제 1

다음 코드의 출력 결과는 무엇인가요?

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] numbers = { 10, 20, 30, 40, 50 };
        Console.WriteLine(numbers[0]);
        Console.WriteLine(numbers[3]);
        Console.WriteLine(numbers.Length);
    }
}
```

<details>
<summary>정답 보기</summary>

```
10
40
5
```

`numbers[0]`은 첫 번째 값 `10`, `numbers[3]`은 네 번째 값 `40`, `.Length`는 칸 수 `5`입니다.

</details>

---

### 문제 2

다음 중 올바른 배열 선언을 고르세요.

```
① int[] arr = new int(5);
② int arr[] = new int[5];
③ int[] arr = new int[5];
④ int[5] arr = new int[];
```

<details>
<summary>정답 보기</summary>

**③ `int[] arr = new int[5];`**

C#에서 배열은 자료형 바로 뒤에 `[]`를 붙이고, `new 자료형[크기]`로 만듭니다.  
① 소괄호`()`가 아니라 대괄호`[]`를 써야 합니다.  
② Java 스타일로, C#에서는 사용할 수 없습니다.  
④ 크기는 `new` 뒤에 지정해야 합니다.

</details>

---

### 문제 3

다음 빈칸을 채워서 colors 배열의 모든 값을 출력하는 코드를 완성하세요.

```csharp
string[] colors = { "빨강", "파랑", "노랑", "초록" };

foreach (________ color in colors)
{
    Console.WriteLine(color);
}
```

<details>
<summary>정답 보기</summary>

```csharp
foreach (string color in colors)
{
    Console.WriteLine(color);
}
```

**실행 결과**
```
빨강
파랑
노랑
초록
```

`colors`는 `string` 배열이므로 `foreach` 변수 자료형도 `string`으로 써야 합니다.

</details>

---

### 문제 4

다음 배열의 모든 수를 더해서 합계를 출력하는 코드를 완성하세요.

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] numbers = { 3, 7, 2, 9, 4 };
        int sum = 0;

        foreach (int n in numbers)
        {
            ________
        }

        Console.WriteLine($"합계: {sum}");
    }
}
```

출력 결과: `합계: 25`

<details>
<summary>정답 보기</summary>

```csharp
sum += n;
```

`+=` 는 `sum = sum + n` 과 같습니다.  
반복문이 돌 때마다 `n`의 값이 하나씩 더해집니다.

</details>

---

### 문제 5

다음 코드에서 **잘못된 부분을 3곳** 찾아서 수정하세요.

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] arr = new int(3);  // ①
        arr[0] = 10;
        arr[1] = 20;
        arr[3] = 30;             // ②

        Console.WriteLine(arr.length); // ③
    }
}
```

<details>
<summary>정답 보기</summary>

① `new int(3)` → `new int[3]` — 배열은 소괄호`()`가 아닌 대괄호`[]`를 사용합니다.  
② `arr[3] = 30` → `arr[2] = 30` — 크기 3짜리 배열의 인덱스는 0, 1, 2 까지입니다. `arr[3]`은 범위 초과입니다.  
③ `arr.length` → `arr.Length` — C#은 대소문자를 구분합니다. `L`은 대문자로 써야 합니다.

**수정된 코드:**
```csharp
int[] arr = new int[3];
arr[0] = 10;
arr[1] = 20;
arr[2] = 30;
Console.WriteLine(arr.Length);
```

**실행 결과:** `3`

</details>

---

### 문제 6 (심화)

5명의 시험 점수 `{ 72, 88, 95, 63, 80 }`를 배열에 저장하고,  
점수를 높은 순서대로 정렬한 뒤 등수와 함께 출력하는 코드를 작성하세요.

```
출력 결과:
1등: 95점
2등: 88점
3등: 80점
4등: 72점
5등: 63점
```

<details>
<summary>정답 보기</summary>

```csharp
using System;
class Hello
{
    public static void Main()
    {
        int[] scores = { 72, 88, 95, 63, 80 };

        Array.Sort(scores);    // 오름차순 정렬
        Array.Reverse(scores); // 내림차순으로 뒤집기

        for (int i = 0; i < scores.Length; i++)
        {
            Console.WriteLine($"{i + 1}등: {scores[i]}점");
        }
    }
}
```

</details>

---

> 📌 **Tip:**
> - 배열의 인덱스는 **0부터 시작**합니다. 크기가 5인 배열의 마지막 인덱스는 **4**입니다.
> - 평균을 구할 때는 **`(double)sum / 배열.Length`** 처럼 형 변환을 해야 소수점이 나옵니다.
> - 배열의 모든 값을 읽을 때는 **`foreach`** 가 편리합니다.
> - 배열을 내림차순으로 정렬하려면 **`Array.Sort()` 후 `Array.Reverse()`** 를 사용하세요.
